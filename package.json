{
  "name": "conversationHEATH weather coding test",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"cd ./frontend && npm run start\" \"cd ./backend && npm run start\"",
    "install": "npm --cwd frontend install && npm --cwd backend install"
  },
  "devDependencies": {
    "concurrently": "^7.0.0"
  },
  "dependencies": {}
}
